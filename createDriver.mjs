import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { const email = 'sofor' + Math.floor(Math.random()*1000) + '@test.com'; const password = 'password123'; console.log('Creating: ' + email); const { data, error } = await supabase.auth.signUp({ email, password, options: { data: { full_name: 'Otomatik Sofor' } } }); if (error) return console.error(error); await new Promise(r => setTimeout(r, 2000)); const { error: updErr } = await supabase.from('users').update({ role: 'driver' }).eq('id', data.user.id); if (updErr) console.log('Update failed due to RLS, please update role in UI manually for ' + email); else console.log('Success! Email: ' + email + ' Password: ' + password); } run();
